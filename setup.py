import os
from setuptools import setup, find_packages

setup(
    name="nams",
    # mandatory
    version="0.1",
    # mandatory     
    author="Eric J. Ma, Mridul Seth",
    packages=find_packages(),
)
